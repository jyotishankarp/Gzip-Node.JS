{
  "name": "gzip-node.js",
  "version": "1.0.0",
  "description": "This repository contains a Node.js module that implements the Gzip compression algorithm. It can be used to compress and decompress data streams using the zlib library. It also provides a command-line interface and a simple web server example.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.18.2"
  }
}
